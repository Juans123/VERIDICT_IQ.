# Evidencias de resultados

Las carpetas `figures/`, `metrics/` y `model_card/` contienen los artefactos congelados que respaldan el informe S5.

Estos resultados corresponden a la ejecución sintética documentada. Nuevas ejecuciones locales se guardan en `artifacts/latest/` y no reemplazan automáticamente estas evidencias.
